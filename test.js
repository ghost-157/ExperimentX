const dPattern = require('./PatternX')
const debug1 = require('./debug')

//for testing purpose we are creating the instance for the patternX
const pattern2 = new dPattern(debug1)

module.exports = pattern2